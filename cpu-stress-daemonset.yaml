apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: cpu-stress
spec:
  selector:
    matchLabels:
      app: cpu-stress
  template:
    metadata:
      labels:
        app: cpu-stress
    spec:
      containers:
      - name: cpu-stress
        image: busybox:1.36
        command:
        - /bin/sh
        - -c
        - |
          while true; do
            :;
          done